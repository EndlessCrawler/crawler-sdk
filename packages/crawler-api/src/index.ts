/**
 * The on-chain contract layer (viem 2): typed contracts, parsed reads, the
 * live mint watcher + per-schema payload assemblers, `formatViewData`.
 *
 * @module @avante/crawler-api
 */
export * from './lib';
